<Window x:Class="WPFAddDemo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="EQE 平台控制系统" Height="600" Width="950" 
        Icon="Data/1.ico"
        WindowStartupLocation="CenterScreen"
        Background="#F0F2F5" FontFamily="Microsoft YaHei, Segoe UI"
        UseLayoutRounding="True" 
        SnapsToDevicePixels="True"
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextRenderingMode="ClearType">

    <Window.Resources>
        <Style x:Key="PosButtonStyle" TargetType="Button">
            <Setter Property="Margin" Value="10"/>
            <Setter Property="FontSize" Value="36"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="BorderBrush" Value="#DCDFE6"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                SnapsToDevicePixels="True">
                            <Border.Effect>
                                <DropShadowEffect Color="#000000" Opacity="0.08" BlurRadius="4" ShadowDepth="1"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" RecognizesAccessKey="True"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#ECF5FF"/>
                                <Setter Property="BorderBrush" Value="#C6E2FF"/>
                                <Setter Property="Foreground" Value="#409EFF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#3A8EE6"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Margin" Value="0,10,0,10"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Background" Value="#409EFF"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4" SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#66B1FF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#3A8EE6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="InputTextBoxStyle" TargetType="TextBox">
            <Setter Property="Height" Value="32"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="6,0"/>
            <Setter Property="BorderBrush" Value="#C0C4CC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="0,5,0,15"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border CornerRadius="3" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" Background="White" SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="0"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2.5*"></ColumnDefinition>
            <ColumnDefinition Width="1*"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Margin="0,0,15,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="1.2*"></RowDefinition>
                <RowDefinition Height="1*"></RowDefinition>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="White" BorderBrush="#E4E7ED" BorderThickness="1" CornerRadius="6" Margin="0,0,0,15" SnapsToDevicePixels="True">
                <Grid Margin="15">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="1*"/>
                        <RowDefinition Height="1*"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Grid.ColumnSpan="3" Text="样品台工位控制" FontSize="18" FontWeight="Bold" Foreground="#111111" Margin="5,0,0,10"/>

                    <Button Grid.Row="2" Grid.Column="2" Content="1" Style="{StaticResource PosButtonStyle}" Command="{Binding Move21PosCommand}"/>
                    <Button Grid.Row="2" Grid.Column="1" Content="2" Style="{StaticResource PosButtonStyle}" Command="{Binding Move22PosCommand}"/>
                    <Button Grid.Row="2" Grid.Column="0" Content="3" Style="{StaticResource PosButtonStyle}" Command="{Binding Move23PosCommand}"/>

                    <Button Grid.Row="1" Grid.Column="2" Content="4" Style="{StaticResource PosButtonStyle}" Command="{Binding Move24PosCommand}"/>
                    <Button Grid.Row="1" Grid.Column="1" Content="5" Style="{StaticResource PosButtonStyle}" Command="{Binding Move25PosCommand}"/>
                    <Button Grid.Row="1" Grid.Column="0" Content="6" Style="{StaticResource PosButtonStyle}" Command="{Binding Move26PosCommand}"/>
                </Grid>
            </Border>

            <Border Grid.Row="1" Background="#1E1E1E" BorderBrush="#333333" BorderThickness="1" CornerRadius="6" SnapsToDevicePixels="True">
                <Grid Margin="15">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="系统运行日志" FontSize="15" FontWeight="Bold" Foreground="#CCCCCC" Margin="0,0,0,10"/>
                    <ScrollViewer Grid.Row="1" x:Name="scrollViewer" VerticalScrollBarVisibility="Auto">
                        <TextBlock Text="{Binding InfoViewModel.PresentedInfo}" Foreground="#4AF626" FontSize="14" TextWrapping="Wrap" FontFamily="Consolas, Courier New"/>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>

        <Border Grid.Column="1" Background="White" BorderBrush="#E4E7ED" BorderThickness="1" CornerRadius="6" SnapsToDevicePixels="True">
            <StackPanel Margin="20">
                <TextBlock Text="系统操作" FontSize="18" FontWeight="Bold" Foreground="#111111" Margin="0,5,0,15"/>
                <Button Content="系统初始化 (Init)" Style="{StaticResource ActionButtonStyle}" Command="{Binding InitCommand}"/>
                <Button Content="平台复位 (Reset)" Style="{StaticResource ActionButtonStyle}" Command="{Binding ResetCommand}" Background="#E6A23C"/>

                <Border Height="1" Background="#E4E7ED" Margin="0,20,0,20" SnapsToDevicePixels="True"/>

                <TextBlock Text="初始工位参数配置" FontSize="18" FontWeight="Bold" Foreground="#111111" Margin="0,0,0,15"/>

                <TextBlock Text="X轴 原点偏移脉冲(前-后+):" Foreground="#333333" FontSize="14" FontWeight="Bold"/>
                <TextBox Style="{StaticResource InputTextBoxStyle}" Text="{Binding UIBindingPulseX, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Text="Y轴 原点偏移脉冲(左-右+):" Foreground="#333333" FontSize="14" FontWeight="Bold"/>
                <TextBox Style="{StaticResource InputTextBoxStyle}" Text="{Binding UIBindingPulseY, UpdateSourceTrigger=PropertyChanged}"/>

                <Button Content="保存参数" Style="{StaticResource ActionButtonStyle}" Background="#67C23A" Command="{Binding SaveSettingsCommand}" Margin="0,10,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>